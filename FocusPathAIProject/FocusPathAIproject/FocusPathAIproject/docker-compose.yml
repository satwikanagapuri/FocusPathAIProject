services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: focuspath
      POSTGRES_PASSWORD: focuspath
      POSTGRES_DB: focuspath_ai
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

